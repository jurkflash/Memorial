@model Memorial.ViewModels.QuadrangleTransactionsFormViewModel
@{
    ViewBag.Title = "Form";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@{Html.RenderAction("ApplicantBrief", "Applicants", new { id = Model.QuadrangleTransactionDto.ApplicantId });}

<h2>Form</h2>

@using (Html.BeginForm("Save", "QuadrangleManage"))
{
    @Html.LabelFor(c => c.QuadrangleTransactionDto.AF)
    <div class="form-group row">
        <div class="col-md-3">
            From
        </div>
        <div class="col-md-6">
            @Html.TextBoxFor(c => c.QuadrangleTransactionDto.FromDate, "{0:yyyy-MM}", new { @class = "form-control datetimepicker", @required = "required" })
            @Html.ValidationMessageFor(c => c.QuadrangleTransactionDto.FromDate)
        </div>
    </div>
    <div class="form-group row">
        <div class="col-md-3">
            To
        </div>
        <div class="col-md-6">
            @Html.TextBoxFor(c => c.QuadrangleTransactionDto.ToDate, "{0:yyyy-MM}", new { @class = "form-control datetimepicker", @required = "required" })
            @Html.ValidationMessageFor(c => c.QuadrangleTransactionDto.ToDate)
        </div>
    </div>
    <div class="form-group row">
        <div class="col-md-3">
            Price
        </div>
        <div class="col-md-6">
            @Html.TextBoxFor(c => c.QuadrangleTransactionDto.Price, new { @class = "form-control", @required = "required" })
        </div>
    </div>
    <div class="form-group row">
        <div class="col-md-3">
            Remark
        </div>
        <div class="col-md-6">
            @Html.TextAreaFor(c => c.QuadrangleTransactionDto.Remark, new { @class = "form-control" })
        </div>
    </div>

    @Html.HiddenFor(c => c.QuadrangleTransactionDto.QuadrangleItemId)
    @Html.HiddenFor(c => c.QuadrangleTransactionDto.QuadrangleId)
    @Html.HiddenFor(c => c.QuadrangleTransactionDto.ApplicantId)
    <button type="submit" class="btn btn-primary">Save</button>
}

@section scripts
{
    @Scripts.Render("~/bundles/jqueryval")

    @Scripts.Render("~/bundles/jqueryui")
    @Styles.Render("~/Content/jqueryuicss")

    <script type="text/javascript">
        $("#QuadrangleTransactionDto_FromDate").blur(function () {
            getAmount();
        });

        $("#QuadrangleTransactionDto_ToDate").blur(function () {
            getAmount();
        });

        function getAmount() {
            var f = $("#QuadrangleTransactionDto_FromDate").val();
            var t = $("#QuadrangleTransactionDto_ToDate").val();
            if (f != "" && t != "") {
                var ii = $("#QuadrangleTransactionDto_QuadrangleItemId").val();
                var url = '/api/Quadrangle/Manages/GetAmount';
                $.ajax({
                    url: url,
                    type: 'GET',
                    data: { from: f, to: t, itemId: ii }
                }).done(function (data) {
                    $("#QuadrangleTransactionDto_Price").val(data);
                }).fail(function () {
                    toastr.error("Something unexpected happened.");
                });
            }
        }

        $(document).ready(function () {   // will trigger when the document is ready
            var dateNow = new Date();
            $('.datepicker').datepicker({
                dateFormat: 'yy-mm',
                useCurrent: true,
                defaultDate: dateNow
            }); //Initialise any date pickers
            $('.datetimepicker').datepicker({
                dateFormat: 'yy-mm',
                useCurrent: true,
                defaultDate: dateNow
            }); //Initialise any date pickers
        });
    </script>
}
