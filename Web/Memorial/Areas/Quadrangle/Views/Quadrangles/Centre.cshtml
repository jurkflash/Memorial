@model Memorial.ViewModels.QuadrangleCentreIndexesViewModel
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var quadrangleCentre in Model.QuadrangleCentreDtos)
        {
            <tr>
                <td>@Html.ActionLink(quadrangleCentre.Name, "Area", new { centreId = quadrangleCentre.Id, applicantId = Model.ApplicantId, area = "Quadrangle" })</td>
                <td>@quadrangleCentre.Description</td>
            </tr>
        }
    </tbody>
</table>


